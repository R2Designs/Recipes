import { notImplemented } from '../_lib/notImplemented.js'

/** Phase 2: proxies Swiggy MCP's `get_addresses` for the signed-in session. */
export default function handler(): Response {
  return notImplemented('fetching saved addresses')
}
