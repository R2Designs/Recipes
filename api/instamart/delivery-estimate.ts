import { notImplemented } from '../_lib/notImplemented.js'

/** Phase 2/4: serviceability + ETA for a pincode. */
export default function handler(): Response {
  return notImplemented('checking delivery estimates')
}
